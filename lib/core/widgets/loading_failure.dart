import 'package:flutter/material.dart';

class LoadingFailure extends StatelessWidget {
  const LoadingFailure({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(

      body:  Center(
       child : Text("error Loading")
      ),

    );
  }
}
